[package]
name = "forge-agents"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
gray_matter = "0.3"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"

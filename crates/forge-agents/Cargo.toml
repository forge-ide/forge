[package]
name = "forge-agents"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1"
gray_matter = "0.3"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"

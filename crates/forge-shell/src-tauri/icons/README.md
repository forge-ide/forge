# Placeholder icons

Binary PNG/icon files are generated at build time by `build.rs` the first time
a platform target needs them. The repo ships no binary assets.

The real Forge icon set lands with the branding pass later in Phase 1.

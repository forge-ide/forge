# LATER.md

<!-- **AGENTS CAN EXCLUDE THIS FILE FROM COMMITS** -->

> Deferred items from the initial Forge rebranding pass. Each entry has a file location, what was left as a placeholder, and what needs to happen before it can be resolved.

---

## product.json — Windows installer GUIDs

**File:** `product.json` lines 19–22

The four Windows installer GUIDs (`win32x64AppId`, `win32arm64AppId`, `win32x64UserAppId`, `win32arm64UserAppId`) are still the VS Code OSS values. These control Inno Setup's upgrade/uninstall behavior — two installations with the same GUID will conflict.

**Action:** Generate four fresh GUIDs (e.g. `uuidgen` or any online generator) and replace the current values before building the Windows installer.

---

## product.json — macOS profile UUIDs

**File:** `product.json` lines 28–29

`darwinProfileUUID` and `darwinProfilePayloadUUID` are still the VS Code OSS values. These are used in the macOS configuration profile payload.

**Action:** Generate two fresh UUIDs and replace before building the macOS package.

---

## Linux package distribution infrastructure

**Files:**
- `resources/linux/debian/postinst.template` lines 94, 105
- `resources/linux/rpm/code.spec.template` line 57

The apt/yum repository URL (`https://packages.forge-ide.dev/repos/forge`) and GPG signing key are placeholders. The embedded Microsoft PGP key was removed and replaced with a `# TODO` comment.

**Action:** Set up Forge package distribution infrastructure (apt + rpm repos, domain, GPG keypair). Then:
1. Replace `https://packages.forge-ide.dev/repos/forge` with the real repo URL in `postinst.template`
2. Insert the real Forge GPG public key in place of the `# echo "<FORGE_GPG_KEY>"` placeholder
3. Update `forge.sources` / `forge.list` filenames if the repo name changes
4. Uncomment and update the yum repo block in `code.spec.template` once the RPM repo is signed

---

## Linux package distribution — GPG key file

**Files:**
- `resources/linux/debian/postinst.template`
- `resources/linux/debian/postrm.template`

Key file paths changed to `forge.gpg` / `forge.sources` / `forge.list` as placeholders. These will only work once the actual Forge signing key is in place.

**Action:** Resolved as part of the distribution infrastructure item above.

---

## WSL extension ID

**Files:**
- `resources/win32/bin/code.sh` line 44
- `resources/win32/versioned/bin/code.sh` line 45

```
WSL_EXT_ID="ms-vscode-remote.remote-wsl"
```

Currently points to Microsoft's Remote - WSL extension. Forge may want to ship its own WSL extension or endorse a third-party one.

**Action:** Decide whether Forge maintains a WSL extension. If yes, publish it and update this ID. If no, document that users should install `ms-vscode-remote.remote-wsl` manually, and consider whether to keep the current value as a convenience default.

---

## Windows AppX publisher identity

**File:** `resources/win32/appx/AppxManifest.xml` line 18

```xml
Publisher="CN=Jeff Roche"
```

This is a minimal placeholder. Publishing to the Microsoft Store requires a publisher identity that exactly matches a verified Microsoft Partner Center account (full DN string with O, L, S, C fields).

**Action:** Create/verify a Partner Center publisher account and replace this value with the exact verified publisher string before any Store submission.

---

## Linux AppData screenshot

**File:** `resources/linux/code.appdata.xml` line 14

Screenshot URL set to:
```
https://github.com/forge-ide/forge/raw/main/resources/forge/screenshot-linux.png
```

This file does not exist yet. AppStream validators will warn on a broken screenshot URL.

**Action:** Take a screenshot of Forge IDE running on Linux, add it to `resources/forge/screenshot-linux.png`, and commit it.

---

## Forge docs pages

**File:** `resources/server/bin/helpers/check-requirements-linux.sh` lines 110, 158

Two error messages now reference docs that don't exist yet:
- `https://github.com/forge-ide/forge/blob/main/docs/supported-linux-distros.md`
- `https://github.com/forge-ide/forge/blob/main/docs/faq-old-linux.md`

**Action:** Create these two docs pages (or redirect them) before shipping the Linux server package publicly.

---

## darwin/code.icns — 1024px frame upscaled

**File:** `resources/darwin/code.icns`

The `ic10` (1024×1024) frame in the ICNS was generated by upscaling the 512px SVG. macOS uses the 1024px frame for Retina displays at 512px logical size. Upscaling from SVG is lossless since SVGs are vector, so this is not a quality issue — but it is worth noting.

**Action:** No immediate action required. If a native 1024px SVG source is ever added to the forge-logos set, regenerate `code.icns` using that source for the `ic10` frame.

---

## Maintainer contact email

**Files:**
- `resources/linux/debian/control.template`
- `resources/linux/rpm/code.spec.template`

Maintainer email set to `jeff@forge-ide.dev`. This domain may not be registered or have mail configured yet.

**Action:** Verify `forge-ide.dev` is registered and the `jeff@` address receives mail before publishing packages. Update if a different contact address is preferred.

---

## Git diff context — IPC-based implementation

**Files:**
- `src/vs/workbench/services/forge/browser/forgeGitDiffService.ts` (current browser stub — returns "unavailable")
- `src/vs/workbench/services/forge/node/contextProviders/forgeGitDiffContextProvider.ts` (working Node.js implementation — uses `child_process`, cannot load in sandboxed renderer)

The `@` context picker offers a "Git Diff" option, but it currently returns a no-op result because the real implementation (`child_process` + `git diff HEAD`) can't run in the Electron renderer. The node implementation was removed from `workbench.desktop.main.ts` to fix a startup crash.

**Action:** Replace the browser stub with a desktop implementation that uses the existing `IProcessService` IPC proxy (already wired in `workbench.desktop.main.ts`). The browser stub injects `IProcessService`, calls `exec('git', ['diff', 'HEAD'])` through it, and parses the result. The node implementation file can then be deleted or kept as a reference.

---

*Last updated: 2026-03-30 — added git diff IPC item.*

---

## Phase 4 Follow-ups

### Wire forgeChatAgentBanner into forgeChatView.ts

**Files:**
- `src/vs/workbench/browser/parts/editor/forgeChat/forgeChatAgentBanner.ts` — exports `renderAgentBanner`, `updateAgentBanner`, `appendAgentStep`
- `src/vs/workbench/browser/parts/editor/forgeChat/forgeChatView.ts` — spawns sub-agents via `IForgeAgentService` but does not render the banner

`forgeChatAgentBanner.ts` is fully implemented but not integrated. When the chat view spawns sub-agents, there is no live visual feedback about agent progress.

**Action:** In `forgeChatView.ts`, subscribe to `IForgeAgentService.onDidChangeAgent` and `onDidAgentStep`, then call `renderAgentBanner` / `updateAgentBanner` / `appendAgentStep` accordingly to render a live agent progress banner in the chat view.

---

### Implement allowedTools filtering in ForgeAgentService

**File:** `src/vs/workbench/services/forge/browser/forgeAgentService.ts`

`ForgeAgentTask.allowedTools` is populated from agent definitions but never applied in `runAgentLoop`. All tools from `forgeMcpService.listTools()` are passed to the agent regardless of what `allowedTools` specifies.

**Action:** In `runAgentLoop`, when `allowedTools` is set on the task, filter the tools list returned by `forgeMcpService.listTools()` to only include tools whose names appear in `allowedTools` before passing the list to the model.

## Scope

Area: `crates/forge-core`. Threat class: **T1 — approval-id guessing**. 64 bits is cryptographically strong against collisions and remote guessing, but in combination with H8 (UDS socket perms), a local attacker could theoretically attempt online guessing at IPC speed. Informational today; cheap to fix.

## Finding

- **Location:** `crates/forge-core/src/ids.rs:13-16`
- **Severity:** low
- **Threat class:** T1

`ToolCallId::new()` draws 8 bytes from `rand::thread_rng()` (ChaCha20 — cryptographically strong source). 64 bits is sufficient for collision avoidance and remote unguessability. On a shared local host where H8's UDS-permission defect allows cross-user UDS connections, an attacker could attempt online guessing at IPC speed.

In isolation this is observational. In combination with H8 it becomes part of the attack path.

## Reproduction

N/A — observational.

## Remediation

Bump to 128 bits (16 bytes) when touching `ids.rs` next. Low cost, raises brute-force resistance against adjacent findings.

## References

- `ids.rs:13-16`

## Definition of Done

- [ ] `ToolCallId` uses 128-bit entropy (16 bytes) — applied at next `ids.rs` touch
- [ ] No regression test needed (no behavior change observable to callers)
- [ ] Tests pass in CI
